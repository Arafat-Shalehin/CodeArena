// Languages pages colors

export const getColor = (index) => {
    if (index === 0) return 'text-success'
    if (index === 1) return 'text-warning'
    if (index === 2) return 'text-error'
    return ' '
}
